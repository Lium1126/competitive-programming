#include <iostream>
using namespace std;

#define rep(i, n) for (int i = 0;i < n;i++)
#define ll long long

int main() {
    double a, b;
    cin >> a >> b;
    cout << a * (b / 100) << endl;
    return(0);
}
